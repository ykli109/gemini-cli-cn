{
  "type": "object",
  "properties": {
    "command": {
      "description": "要作为 `bash -c <command>` 执行的精确 bash 命令",
      "type": "string"
    },
    "description": {
      "description": "命令的简要描述，供用户参考。请具体和简洁。理想情况下为一句话。为了清晰起见，最多可包含三句话。无换行符。",
      "type": "string"
    },
    "directory": {
      "description": "（可选）运行命令的目录，如果不是项目根目录。必须是相对于项目根目录的路径，并且必须已经存在。",
      "type": "string"
    }
  },
  "required": ["command"]
}